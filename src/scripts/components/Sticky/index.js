import css from 'react-css-modules';

import style from './style';

const { string, bool, node } = PropTypes;

const Sticky = (props) => {
  return (
    <div styleName='root'>
    </div>
  );
};

export default css(Sticky, style);
