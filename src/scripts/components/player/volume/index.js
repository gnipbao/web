import css from 'react-css-modules';

import style from './style';

const { bool, func, object, shape } = PropTypes;

const Volume = (props) => {
  return (
    <div styleName='root'>
    </div>
  );
};

export default css(Volume, style);
