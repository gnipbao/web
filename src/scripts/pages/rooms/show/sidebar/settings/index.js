import css from 'react-css-modules';

import style from './style';

const { bool, object, func } = PropTypes;

const Settings = (props) => { 
  return (
    <div styleName='settings'>
    </div>
  );
}

export default css(Settings, style);
